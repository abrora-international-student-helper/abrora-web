export default function StatusCard() {
  return (
    <div className="status-card">
      {/* TODO: Visa status display card */}
      {/* - Current visa status (F-1, J-1, etc.) */}
      {/* - Days remaining in status */}
      {/* - Status health indicator (OK/Warning/Alert) */}
      {/* - Visual progress bar */}
    </div>
  )
}
